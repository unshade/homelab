apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: longhorn-system

resources:
  - namespace.yaml
  - secret-sops.yaml
  - release.yaml
  - storage-class.yaml
  - storage-class-replicated.yaml
  - storage-class-single-replica.yaml
  - recurring-job-backup.yaml
  - backup-target.yaml
