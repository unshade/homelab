---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-config
spec:
  storageClassName: longhorn-replicated
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
